apiVersion: apps/v1
kind: Deployment
metadata:
  name: otot-a2
  labels:
    app: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: nodeapp
          image: tzytzytzy/otot-a:latest
          ports:
            - name: http
              containerPort: 8080
          resources:
            limits:
              cpu: 40m
              memory: 100Mi